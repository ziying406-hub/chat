# 生产联调验证（2026-09-10）

目标：`https://999.99chat99.com`。使用独立新注册账号和临时群，不修改业务群。
本报告只覆盖此前列出的消息、媒体、头像、好友、收藏、通知及换浏览器恢复，不代表所有功能或所有浏览器均已验收。

## 已通过

`99chat/production_smoke_e2e.cjs` 最终完整运行退出码 0：

- 两个独立 Chromium 上申请好友、接收申请、同意并进入私聊。
- 文字送达对方；刷新后会话和历史仍可读取；双向发送成功。
- 图片上传后对方真实解码显示，刷新后继续显示。
- 录制 3 秒语音，对方显示时长、真实媒体播放时间前进；暂停后音频确实暂停。
- 取消录音后没有额外发送语音；已发送语音刷新后仍存在。
- 个人头像上传、点击保存、刷新后保留。
- 群主在群管理设置上传头像并刷新保留；成员可看头像，无更换按钮。
- 文字、图片、语音收藏均通过真实 API 查到；另一个账号收藏为空。
- 全新浏览器登录能读到收藏，打开图片详情并解码、播放语音详情；聊天文字和图片历史恢复。
- 站内收到消息后，原生 AudioContext/oscillator 实际运行。测试没有替换成模拟音频对象。
- 临时群最终解散。测试账号和其测试消息/上传文件保留；不删除业务数据。

录音使用 Chromium 的测试麦克风，验证了 MediaRecorder、上传、送达、解码和播放链路，不代表已人工听测物理麦克风/扬声器，也不代表 Safari/iOS 已通过。

运行方式（凭据配置只通过环境注入）：

```sh
cd 99chat
E2E_BASE=https://999.99chat99.com TEST_VERIFY_CODE=<环境验证码> node production_smoke_e2e.cjs
```

## 本次修复

1. Chat 健康探针调用不存在的 `mage`，造成退出 127 的误报。改为已有只读 API 检查；实际正例和连接失败/404/业务错误负例验证通过。
2. 生产缺少原有收藏补丁，接口返回 404。部署仅替换 chat-api 的镜像；鉴权、账号隔离、幂等更新、删除和重启持久化验证通过。见 [收藏部署记录](../server-patches/chat/FAVORITES.md)。
3. 语音发送回调读取开始录音时的旧时长。改用本次录音实际耗时；取消时解除发送回调；暂停操作真正暂停音频，并处理播放失败。

前端已生产构建并部署。`openim-server` 权限补丁镜像和容器未变。本地 `npm run build`、`voice_message_duration_test.cjs` 通过。

## 尚不能判定为完成的范围

- **关闭网页后的离线推送**：真实浏览器报 `Failed to resolve module specifier 'firebase/messaging'`。现有权限开关不等于推送接通，需要 Firebase Web 配置、推送 Service Worker 与服务端配置联调；已向用户询问配置。
- **同账号两个 Web 同时在线**：当前服务端 `multiLogin.policy: 1` 为同平台单实例。此次验证是顺序换浏览器恢复，没有变更全站登录策略，也不声称双 Web 同时在线通过。
- **历史本机收藏迁移**：上线前仅在 localStorage 保存的记录不会自动上传。其本机数据未删除；当前新收藏已写服务端。不要把新收藏多端验证外推为旧记录已迁移。
- 浏览器物理提示音、移动端震动、关闭网页后的系统通知，没有本轮真实设备证据。

中途发现的测试问题（文本定位同时命中侧栏、保存后过早跳转、重新登录使旧测试 token 失效）已修正测试等待和 token 更新；不作为产品修复计数。另有一轮刷新后的即时收信超时，最终双向握手后的完整回归通过，但未据此声称已完成长时间断网/重连压力验证。
